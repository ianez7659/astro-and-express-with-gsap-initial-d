{
  "name": "astro-express-vercel",
  "version": "1.0.0",
  "description": "Astro + Express project for Vercel deployment",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  }
}
